import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-early',
  imports: [RouterLink],
  templateUrl: './early.html',
  styleUrl: './early.css',
})
export class Early {

}
